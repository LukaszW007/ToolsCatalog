const React = require('react');
const ReactDOM = require('react-dom');

class header extends React.Component {
    constructor (props) {
        super(props);

    }

    render () {
        return (
            <div></div>
        )


    };
}